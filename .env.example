# Runtime integrations remain disabled until privacy and consent requirements
# are confirmed. Never commit real credentials or private operational data.
# Song request currently uses an in-file mock (pages/playlist/api/createSongRequest.ts);
# set this once the backend endpoint exists.
VITE_API_BASE_URL=
VITE_TELEMETRY_ENABLED=false
VITE_GA_MEASUREMENT_ID=
VITE_SENTRY_DSN=
VITE_CLARITY_PROJECT_ID=
