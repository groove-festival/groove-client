# AI worklog — 2026-08

### 2026-08-31 - AI Agent Workflow 경량화

- 담당자: 사용자(이름 미기재)
- 사용한 Agent / Skill: Codex, `project-workflow`, `project-ai-worklog`,
  `project-quality-gates`, `skill-creator`, `documentation-harness`
- 사용 목적: AI 작업의 추적 가능성과 FSD 검증을 유지하면서 상시 문서 로딩,
  과도한 Skill 연쇄 호출, 불필요한 전체 검증으로 인한 맥락·실행 비용을 줄임.
- 입력 맥락: 사용자의 경량화 요청과 앞선 비용 검토 결과, `AGENTS.md`,
  `CLAUDE.md`, `CONVENTION.md`, `docs/AI_AGENT_WORKFLOW.md`,
  `docs/FSD_ARCHITECTURE.md`, `package.json`, 관련 canonical Skill 및 동기화
  스크립트.
- AI 제안 또는 산출물: 문서와 Skill의 조건부 라우팅, targeted/fast/full gate,
  월별 작업 기록 분리를 반영함. 주요 변경은 `AGENTS.md`, `CLAUDE.md`,
  `CONVENTION.md`, `docs/AI_AGENT_WORKFLOW.md`, `.agents/skills/`, 생성된
  `.claude/skills/`, `.github/pull_request_template.md`임.
- 사람이 실제로 결정·수정한 내용: 사용자가 앞선 비용 검토에서 제안한
  경량화 방향을 저장소에 적용하도록 요청함.
- 검증 결과:
  - `pnpm format:check`: 첫 실행 실패(PR 템플릿 표 정렬), `pnpm format` 적용
    후 재실행 통과.
  - `powershell -NoProfile -ExecutionPolicy Bypass -File scripts/sync-agent-skills.ps1 -Mode Sync`:
    통과, 관리 Skill 8개 동기화 및 드리프트 없음.
  - 같은 스크립트의 `-Mode Check`: 통과, 관리 Skill 8개 frontmatter 유효 및
    드리프트 없음.
  - `pnpm check:fsd`: 통과.
  - `git diff --check`: 통과.
  - `quick_validate.py`: 미완료. Windows Store Python 별칭은 실행되지 않았고,
    번들 Python에는 `yaml` 모듈이 없어 실패함. 새 의존성을 설치하지 않고
    저장소 동기화 스크립트의 frontmatter·드리프트 검사로 대체함.
  - `pnpm check:fast`, `pnpm check:full`: 미실행. 애플리케이션 코드를 변경하지
    않은 문서·Agent 규칙 작업이므로 build와 E2E를 다시 검증하지 않음.
- 남은 확인 사항: 실제 후속 작업에서 Skill 선택 수와 입력 토큰 사용량이
  예상대로 줄어드는지 관찰해야 함. 제품·모델별 캐시와 실제 과금량은 저장소
  파일만으로 확인할 수 없음.
- 관련 Issue / PR / Discussion: 없음

### 2026-08-31 - Issue·PR 템플릿 단순화

- 담당자: 사용자(이름 미기재)
- 사용한 Agent / Skill: Codex, `documentation-harness`,
  `project-ai-worklog`
- 사용 목적: Issue와 PR 작성 부담을 줄이고 프로젝트에 필요한 최소 맥락과
  검증만 남김.
- 입력 맥락: 사용자가 지정한 Issue 종류와 PR 항목, 기존
  `.github/ISSUE_TEMPLATE/`, `.github/pull_request_template.md`,
  `CONVENTION.md`, `docs/AI_AGENT_WORKFLOW.md`.
- AI 제안 또는 산출물: `디자인`, `리팩토링`, `버그`, `기능`, `문서 작업`
  Issue Form 5개를 최소 필드로 구성하고, PR 템플릿을 관련 이슈, 작업 내용,
  작업 방식의 이유, 문서화 내용, 검증 섹션으로 단순화함. 관련 Workflow 문서도
  같은 의미로 동기화함.
- 사람이 실제로 결정·수정한 내용: 사용자가 Issue 종류 5개와 PR에 포함할
  섹션 5개를 직접 지정함.
- 검증 결과:
  - `pnpm format:check`: 통과, Markdown과 YAML을 포함한 전체 format 확인.
  - PowerShell 구조 검사: 통과, Issue Form 5개와 필수 YAML 필드·중복 ID,
    PR 필수 섹션 5개 및 제거 대상 필드 부재 확인.
  - `git diff --check`: 통과.
  - `pnpm check:fsd`, `pnpm check:fast`, `pnpm check:full`: 미실행.
    프론트엔드 소스나 동작을 변경하지 않은 템플릿·문서 작업임.
- 남은 확인 사항: GitHub Issue·PR 작성 화면의 실제 렌더링은 확인하지 않음.
- 관련 Issue / PR / Discussion: 없음
