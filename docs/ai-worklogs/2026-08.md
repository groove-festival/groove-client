# AI worklog — 2026-08

### 2026-08-31 - AI Agent Workflow 경량화

- 담당자: 사용자(이름 미기재)
- 사용한 Agent / Skill: Codex, `project-workflow`, `project-ai-worklog`,
  `project-quality-gates`, `skill-creator`, `documentation-harness`
- 사용 목적: AI 작업의 추적 가능성과 FSD 검증을 유지하면서 상시 문서 로딩,
  과도한 Skill 연쇄 호출, 불필요한 전체 검증으로 인한 맥락·실행 비용을 줄임.
- 입력 맥락: 사용자의 경량화 요청과 앞선 비용 검토 결과, `AGENTS.md`,
  `CLAUDE.md`, `CONVENTION.md`, `docs/AI_AGENT_WORKFLOW.md`,
  `docs/FSD_ARCHITECTURE.md`, `package.json`, 관련 canonical Skill 및 동기화
  스크립트.
- AI 제안 또는 산출물: 문서와 Skill의 조건부 라우팅, targeted/fast/full gate,
  월별 작업 기록 분리를 반영함. 주요 변경은 `AGENTS.md`, `CLAUDE.md`,
  `CONVENTION.md`, `docs/AI_AGENT_WORKFLOW.md`, `.agents/skills/`, 생성된
  `.claude/skills/`, `.github/pull_request_template.md`임.
- 사람이 실제로 결정·수정한 내용: 사용자가 앞선 비용 검토에서 제안한
  경량화 방향을 저장소에 적용하도록 요청함.
- 검증 결과:
  - `pnpm format:check`: 첫 실행 실패(PR 템플릿 표 정렬), `pnpm format` 적용
    후 재실행 통과.
  - `powershell -NoProfile -ExecutionPolicy Bypass -File scripts/sync-agent-skills.ps1 -Mode Sync`:
    통과, 관리 Skill 8개 동기화 및 드리프트 없음.
  - 같은 스크립트의 `-Mode Check`: 통과, 관리 Skill 8개 frontmatter 유효 및
    드리프트 없음.
  - `pnpm check:fsd`: 통과.
  - `git diff --check`: 통과.
  - `quick_validate.py`: 미완료. Windows Store Python 별칭은 실행되지 않았고,
    번들 Python에는 `yaml` 모듈이 없어 실패함. 새 의존성을 설치하지 않고
    저장소 동기화 스크립트의 frontmatter·드리프트 검사로 대체함.
  - `pnpm check:fast`, `pnpm check:full`: 미실행. 애플리케이션 코드를 변경하지
    않은 문서·Agent 규칙 작업이므로 build와 E2E를 다시 검증하지 않음.
- 남은 확인 사항: 실제 후속 작업에서 Skill 선택 수와 입력 토큰 사용량이
  예상대로 줄어드는지 관찰해야 함. 제품·모델별 캐시와 실제 과금량은 저장소
  파일만으로 확인할 수 없음.
- 관련 Issue / PR / Discussion: 없음

### 2026-08-31 - Issue·PR 템플릿 단순화

- 담당자: 사용자(이름 미기재)
- 사용한 Agent / Skill: Codex, `documentation-harness`,
  `project-ai-worklog`
- 사용 목적: Issue와 PR 작성 부담을 줄이고 프로젝트에 필요한 최소 맥락과
  검증만 남김.
- 입력 맥락: 사용자가 지정한 Issue 종류와 PR 항목, 기존
  `.github/ISSUE_TEMPLATE/`, `.github/pull_request_template.md`,
  `CONVENTION.md`, `docs/AI_AGENT_WORKFLOW.md`.
- AI 제안 또는 산출물: `디자인`, `리팩토링`, `버그`, `기능`, `문서 작업`
  Issue Form 5개를 최소 필드로 구성하고, PR 템플릿을 관련 이슈, 작업 내용,
  작업 방식의 이유, 문서화 내용, 검증 섹션으로 단순화함. 관련 Workflow 문서도
  같은 의미로 동기화함.
- 사람이 실제로 결정·수정한 내용: 사용자가 Issue 종류 5개와 PR에 포함할
  섹션 5개를 직접 지정함.
- 검증 결과:
  - `pnpm format:check`: 통과, Markdown과 YAML을 포함한 전체 format 확인.
  - PowerShell 구조 검사: 통과, Issue Form 5개와 필수 YAML 필드·중복 ID,
    PR 필수 섹션 5개 및 제거 대상 필드 부재 확인.
  - `git diff --check`: 통과.
  - `pnpm check:fsd`, `pnpm check:fast`, `pnpm check:full`: 미실행.
    프론트엔드 소스나 동작을 변경하지 않은 템플릿·문서 작업임.
- 남은 확인 사항: GitHub Issue·PR 작성 화면의 실제 렌더링은 확인하지 않음.
- 관련 Issue / PR / Discussion: 없음

### 2026-08-31 - 팀 Notion 작업 기록 Workflow 구성

- 담당자: 지훈
- 사용한 Agent / Skill: Codex, `project-workflow`,
  `project-ai-worklog`, `project-notion-worklog`, `project-quality-gates`,
  `skill-creator`, `documentation-harness`, `browser`
- 사용 목적: 팀원별 Notion 하위 페이지에 AI 지원 작업과 트러블슈팅을
  간결하게 남기고, commit 이후 push 이전에 실제 기록이 있는지 확인하는
  공용 Workflow를 구성함.
- 입력 맥락: 사용자가 지정한 Notion 상위 페이지와 팀원별 하위 페이지 운영
  방식, post-commit·pre-push 순서, 가독성 검증 요청, `AGENTS.md`,
  `CLAUDE.md`, `CONVENTION.md`, `README.md`, `docs/AI_AGENT_WORKFLOW.md`,
  기존 Skill 동기화 스크립트와 Git hook, 공식 Codex·Claude Code·Notion MCP
  문서, 실제 Notion 페이지 구조.
- AI 제안 또는 산출물: 공용 MCP 설정인 `.codex/config.toml`과 `.mcp.json`,
  canonical `project-notion-worklog` Skill과 Claude 호환 사본, checkout 로컬
  대상·commit별 기록 증거를 관리하는 `scripts/notion-worklog-state.mjs`,
  `.githooks/post-commit`, `.githooks/pre-push`, 관련 문서·package script를
  작성함. Notion의 `개발 문서` 아래에 `지훈` 페이지와 공식 기록이 아님을
  표시한 가독성 검증용 샘플 페이지를 생성함.
- 사람이 실제로 결정·수정한 내용: 사용자가 세 명의 팀원이 각자 이름 하위
  페이지를 사용하고, commit 이후 push 전에 Notion 기록을 남기며, 사람
  검토·승인 상태를 기록하지 않도록 지정함. 이어서 작업을 계속하도록 요청함.
- 검증 결과:
  - Notion MCP `self`·상위 페이지·하위 페이지 조회: 통과. 인증 계정과
    `나영`, `성민` 하위 페이지를 확인하고, 누락된 `지훈` 페이지 생성 후 상위
    페이지를 다시 조회해 세 페이지가 모두 보존된 것을 확인함.
  - Notion 샘플 페이지 생성 후 재조회: 통과. 제목, 목적 callout, `작업 내용`,
    `결정과 이유`, `AI와 정리한 내용`, `검증`, `남은 사항`, 공식 commit
    미연결 표시와 사람 검토 상태 부재를 확인함.
  - `node --check scripts/notion-worklog-state.mjs`: 통과.
  - `pnpm format:check`: 통과.
  - `pnpm lint`: 통과.
  - `powershell -NoProfile -ExecutionPolicy Bypass -File scripts/sync-agent-skills.ps1 -Mode Check`:
    통과, 관리 Skill 9개의 frontmatter와 Codex·Claude 사본 드리프트 없음.
  - `git diff --check`: 통과.
  - Git Bash `sh -n`으로 세 hook 검사: 첫 시도는 `sh`가 PowerShell PATH에
    없어 실패, 설치된 Git Bash의 `sh.exe`를 명시해 재실행 통과.
  - 임시 로컬 Git 저장소 검증: 통과. 초기 기준점, commit 후 대기 상태,
    미기록 push 차단, 기록 표시 후 push 허용, branch 전환 뒤 commit별 증거
    유지, `git -c aiworklog.skip=true push`의 1회 우회를 확인함.
  - `pnpm hooks:install`: 첫 시도는 샌드박스의 `.git/config` 쓰기 제한으로
    실패, 저장소 로컬 설정 쓰기 권한으로 재실행 통과.
  - `pnpm notion:setup -- "<지훈 하위 페이지 URL>"`과
    `pnpm notion:status`: 통과. 개인 URL은 checkout의 `.git/config`에만
    저장되고 기존 HEAD가 기준점으로 표시됨.
  - `quick_validate.py`: 실패. 사용 가능한 번들 Python에 `yaml` 모듈이 없어
    실행하지 못했으며 새 의존성을 추가하지 않음. 저장소의 frontmatter·드리프트
    검사로 대체함.
  - Notion 브라우저 화면 검수: 미실행. 브라우저 사용 한도로 대상 URL 접근이
    차단되어 MCP 재조회까지만 수행함.
  - `pnpm typecheck`, `pnpm test`, `pnpm check:fsd`, `pnpm build`,
    `pnpm test:e2e`: 미실행. 프론트엔드 제품 코드나 FSD 구조를 변경하지 않은
    Workflow·문서·Git hook 작업임.
- 남은 확인 사항: 브라우저 사용이 가능할 때 샘플 첫 화면과 목록 구획의 실제
  렌더링을 확인해야 함. 다음 실제 작업의 최종 commit 뒤 공식 Notion 기록을
  생성하고 해당 기록으로 pre-push guard가 해제되는 전체 흐름은 아직 수행하지
  않음. 다른 두 팀원은 각자의 컴퓨터에서 OAuth 인증과 개인 하위 페이지 설정이
  필요함.
- 관련 Issue / PR / Discussion: 없음
